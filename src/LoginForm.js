const LoginForm = () => {
  return (
    <div>
      <form className="login-form">
        <h3>Login</h3>
        <label>Username</label>
        <input type="text" />
        <label>Password</label>
        <input type="password" />
      </form>
    </div>
  );
};

export default LoginForm;
